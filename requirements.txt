# Core scientific stack (always required)
numpy>=1.24.0
pyyaml>=6.0

# SDR hardware drivers (install when hardware is present)
# pyrtlsdr>=0.3.0        # RTL-SDR (uncomment when hardware available)
# uhd>=4.3.0             # USRP B200 UHD Python bindings

# GNU Radio Python bindings (install with system package manager)
# gnuradio-companion is usually installed as a system package:
#   Ubuntu: sudo apt-get install gnuradio python3-gnuradio

# SNMP trap sending
pysnmp>=6.1.0

# GPS support
pyserial>=3.5          # For direct serial NMEA GPS receivers
# gpsd-py3>=0.3.0       # Uncomment when GPSD daemon is available

# Visualisation (optional – enables PNG waterfall export)
matplotlib>=3.7.0

# Web dashboard (optional)
# flask>=3.0.0           # Uncomment to enable the HTTP dashboard

# Development / testing
pytest>=7.4.0
pytest-cov>=4.1.0
